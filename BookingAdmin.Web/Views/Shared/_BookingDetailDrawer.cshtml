<!-- Detail Drawer Overlay -->
<div class="drawer-overlay" id="drawerOverlay"></div>

<!-- Detail Drawer -->
<div class="detail-drawer" id="detailDrawer">
    <div class="drawer-header">
        <h5 class="drawer-title">Chi Tiết Booking</h5>
        <button type="button" class="btn-close" onclick="closeDetailDrawer()"></button>
    </div>
    <div class="drawer-body" id="detailContent"></div>
    <div class="drawer-footer">
        <button type="button" class="btn btn-sm btn-primary" onclick="editBooking()">
            <i class="bi bi-pencil"></i> Sửa
        </button>
        <button type="button" class="btn btn-sm btn-warning" onclick="transferBooking()">
            <i class="bi bi-arrow-left-right"></i> Chuyển tàu
        </button>
        <button type="button" class="btn btn-sm btn-danger" onclick="cancelBooking()">
            <i class="bi bi-x-circle"></i> Huỷ booking
        </button>
        <button type="button" class="btn btn-sm btn-secondary" onclick="printVoucher()">
            <i class="bi bi-printer"></i> In voucher
        </button>
    </div>
</div>

<style>
    /* Detail Panel Styles */
    .booking-detail-panel {
        font-size: 0.95rem;
    }

    .detail-group {
        margin-bottom: 1rem;
    }

    .group-title {
        font-weight: 600;
        color: #333;
        margin-bottom: 0.75rem;
        padding-bottom: 0.5rem;
        border-bottom: 2px solid #dee2e6;
        text-transform: uppercase;
        font-size: 0.9rem;
        letter-spacing: 0.5px;
    }

    .detail-row {
        display: flex;
        margin-bottom: 0.65rem;
        align-items: flex-start;
    }

    .detail-label {
        font-weight: 500;
        color: #555;
        min-width: 140px;
        flex-shrink: 0;
    }

    .detail-value {
        flex: 1;
        color: #333;
        word-break: break-word;
    }

    .detail-readonly {
        background-color: #f8f9fa;
        padding: 0.4rem 0.6rem;
        border-radius: 3px;
        font-family: monospace;
        font-size: 0.9rem;
        border: 1px solid #e9ecef;
    }

    .detail-price {
        font-weight: 600;
        font-size: 1.1rem;
        padding: 0.6rem 0.8rem;
        border-radius: 4px;
        display: inline-block;
        color: white;
    }

    .status-badge {
        font-weight: 500;
    }

    /* Drawer Styles */
    .drawer-overlay {
        position: fixed;
        inset: 0;
        background: rgba(0, 0, 0, 0.3);
        z-index: 1049;
        opacity: 0;
        pointer-events: none;
        transition: opacity 0.25s ease;
    }

    .drawer-overlay.open {
        opacity: 1;
        pointer-events: auto;
    }

    .detail-drawer {
        position: fixed;
        right: 0;
        top: 0;
        height: 100vh;
        width: 380px;
        background: white;
        transform: translateX(100%);
        transition: transform 0.25s ease;
        z-index: 1050;
        overflow-y: auto;
        display: flex;
        flex-direction: column;
        box-shadow: -2px 0 8px rgba(0, 0, 0, 0.15);
    }

    .detail-drawer.open {
        transform: translateX(0);
    }

    .drawer-header {
        padding: 1.25rem;
        border-bottom: 1px solid #dee2e6;
        display: flex;
        justify-content: space-between;
        align-items: center;
        flex-shrink: 0;
    }

    .drawer-title {
        margin: 0;
        font-size: 1.1rem;
        font-weight: 600;
        color: #333;
    }

    .drawer-body {
        padding: 1.5rem;
        flex: 1;
        overflow-y: auto;
    }

    .drawer-footer {
        padding: 1rem 1.25rem;
        border-top: 1px solid #dee2e6;
        display: flex;
        gap: 0.5rem;
        flex-wrap: wrap;
        flex-shrink: 0;
        background: #f8f9fa;
    }

    .drawer-footer .btn {
        flex: 1;
        min-width: 90px;
    }

    @@media (max-width: 768px) {
        .detail-drawer {
            width: 100%;
        }
    }
</style>
